const config = {
  use: {
    headless: false,
  },
};

module.exports = config;
/*to see what playwright is doing while runnding the tests. 
In the headless mode, the browser is not shown
In default mode, playwright configs with headless mode */
